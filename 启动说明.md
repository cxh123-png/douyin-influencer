# 快速启动指南

## 前置要求

请确保您的系统已安装 Node.js (推荐 v16 或更高版本)

检查是否已安装:
```bash
node -v
npm -v
```

如果未安装，请访问 https://nodejs.org/ 下载安装

## 启动步骤

### 1. 安装依赖

在项目根目录下运行:
```bash
npm install
```

这将安装 Vue 3、Vite 等所有必需的依赖包

### 2. 启动开发服务器

```bash
npm run dev
```

启动成功后，浏览器会自动打开 http://localhost:3000

如果没有自动打开，请手动访问上述地址

### 3. 开始使用

- 浏览不同分类的网红
- 使用搜索框查找特定网红
- 点击卡片查看详情
- 在详情页点击"访问抖音主页"跳转到对应网红的抖音页面

## 常见问题

### Q: npm 命令找不到？
A: 请先安装 Node.js，安装完成后重启终端再试

### Q: 端口 3000 被占用？
A: 修改 `vite.config.js` 中的 `port` 配置为其他端口

### Q: 如何停止服务器？
A: 在终端中按 `Ctrl + C`

### Q: 头像图片不显示？
A: 项目使用的是示例图片链接，您可以替换 `src/data/influencers.js` 中的 avatar URL 为实际图片地址

## 生产部署

构建生产版本:
```bash
npm run build
```

构建完成后，`dist` 目录包含可部署的文件

预览生产构建:
```bash
npm run preview
```

## 技术支持

如有问题，请查看 README.md 获取更多详细信息
